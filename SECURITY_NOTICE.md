# 安全警告

## API密钥泄露处理

如果您在版本历史中发现过API密钥 `5c9b4291a94540878c8fab0cddc8bc71.IlLUPbvGtiW32isQ`：

1. **该密钥已泄露**，不应继续使用
2. **立即在智谱AI平台重新生成新密钥**
3. **更新.env文件**使用新密钥
4. **切勿将.env文件提交到版本控制系统**

## 安全最佳实践

1. 使用 `.env.example` 作为模板
2. 复制为 `.env` 并填入真实密钥
3. 确保 `.env` 在 `.gitignore` 中
4. 永远不要提交 `.env` 文件
5. 永远不要在代码中硬编码API密钥

## 报告安全问题

如果发现新的安全问题，请立即通知项目维护者。
